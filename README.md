# Playtomic — TypeScript + tsup starter

Setup included:

- TypeScript project with `@/*` path alias to `src/`.
- Build with `tsup` using `tsup.config.ts` which maps `@` -> `src`.

Quick start (PowerShell):

```powershell
cd "playtomic-node"
npm install
npm run build
node dist/index.cjs
```
